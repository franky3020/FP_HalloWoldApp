<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar"> <!-- 不顯示 App Action Bar : My_First_Application (DarkActionBar -> NoActionBar) -->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/dark_blue</item> <!-- 改變 Tool Bar 顏色，讓它與背景顏色一致 -->
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
