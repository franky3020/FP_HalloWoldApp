<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar"> <!-- 要使用 Material, 就必須繼承這個 -->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/dark_blue</item> <!-- 改變 Tool Bar 顏色，讓它與背景顏色一致 -->
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeNo" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    <style name="SignTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#00000000</item>
        <item name="colorPrimaryDark">@color/dark_blue</item> <!-- 改變 Tool Bar 顏色，讓它與背景顏色一致 -->
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!--<style name="MySpinnerDatePickerStyle" parent="android:Theme.Material.Dialog">
        <item name="android:datePickerStyle">@style/MySpinnerDatePicker</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"></style>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"></style>


</resources>
